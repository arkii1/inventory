extends layout 

block content 

    h1=title

    form(action='' method='POST')
        input(type='hidden' name='category_id' id='category_id' value=category._id required)
        button(type='submit') Delete